{{- if .Values.services.authoriumDocsProxy.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.services.authoriumDocsProxy.namespace }}
  labels:
    app: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/component: server
    rubix-app: {{ .Chart.Name }}
  {{- with .Values.services.authoriumDocsProxy.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.services.authoriumDocsProxy.service.type | default "ClusterIP" }}
  {{- if eq .Values.services.authoriumDocsProxy.service.type "LoadBalancer" }}
  loadBalancerClass: {{ .Values.services.authoriumDocsProxy.service.loadBalancerClass }}
  {{- end }}
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}
  ports:
    - name: http-svc
      port: {{ .Values.services.authoriumDocsProxy.service.port }}
      targetPort: {{ .Values.services.authoriumDocsProxy.deployment_nginx.containerPort }}
      protocol: TCP
{{ end }}
