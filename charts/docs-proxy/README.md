# authorium-docs-proxy

The [authorium-docs-proxy](https://github.com/AuthoriumDPA/authorium-docs-proxy) service is an internal service created by Authorium to check calls to our docs site through the proxy, which validates that users which should have access to the docs site.
