{{- if and .Values.services .Values.services.analytics .Values.services.analytics.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.services.analytics.namespace }}
  labels:
    app: analytics
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: analytics
    app.kubernetes.io/component: server
    rubix-app: {{ .Chart.Name }}
  {{- with .Values.services.analytics.service.annotations }}    
  annotations: 
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec: 
  type: {{ .Values.services.analytics.service.type | default "ClusterIP" }}
  {{- if eq .Values.services.analytics.service.type "LoadBalancer" }}
  loadBalancerClass: {{ .Values.services.analytics.service.loadBalancerClass }}
  {{- end }}
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: analytics
  ports:
    - name: http-svc
      port: {{ .Values.services.analytics.service.port }}
      targetPort: {{ .Values.services.analytics.deployment_nginx.containerPort }}
      protocol: TCP
{{ end }}
