{{- if .Values.services.formioenterprise.create }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.services.formioenterprise.namespace }}
  labels:
    app: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}
    rubix-app: {{ .Chart.Name }}
  {{- with .Values.services.formioenterprise.deployment.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.services.formioenterprise.deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/name: {{ .Release.Name }}
        app.kubernetes.io/component: server
        rubix-app: {{ .Chart.Name }}
      annotations:
        com.palantir.rubix.pod/sls-service-info-v2: |
          {
            "ServiceName": "{{ .Release.Name }}",
            "ServiceID": "{{ .Release.Namespace }}-{{ .Release.Name }}",
            "StackName": "{{ .Release.Namespace }}",
            "StackID": "{{ .Release.Namespace }}",
            "Containers": {
              "nginx": {
                "ProductName": "{{ .Chart.Name }}",
                "ProductVersion": "{{ .Chart.AppVersion }}"
              },
              "formio-enterprise": {
                "ProductName": "{{ .Chart.Name }}",
                "ProductVersion": "{{ .Chart.AppVersion }}"
              }
            }
          }
        com.palantir.rubix.service/pod-cert: '{}'
    spec:
      automountServiceAccountToken: {{ .Values.services.formioenterprise.serviceaccount.automountServiceAccountToken }}
      serviceAccountName: "{{ .Values.services.formioenterprise.serviceaccount.name }}"
      #securityContext:
      #  runAsUser: 10001
      #  runAsGroup: 10001
      #  fsGroup: 10001
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: com.palantir.rubix/instance-group
                    operator: In
                    values:
                      - fedstart
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/instance: {{ .Release.Name }}
                topologyKey: topology.kubernetes.io/zone
              weight: 100
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app.kubernetes.io/instance
                    operator: In
                    values:
                      - {{ .Release.Name }}
              topologyKey: kubernetes.io/hostname

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/instance: {{ .Release.Name }}
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/instance: {{ .Release.Name }}

      containers:
        - name: nginx
          image: "{{ .Values.services.formioenterprise.deployment_nginx.image.repository }}:{{ .Values.services.formioenterprise.deployment_nginx.image.tag }}"
          imagePullPolicy: {{ .Values.services.formioenterprise.deployment_nginx.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.services.formioenterprise.deployment_nginx.containerPort }}
              protocol: TCP
              name: http-svc
          volumeMounts:
            - name: tls-secret-volume
              mountPath: /etc/ssl/
              readOnly: true                 
          env:
            {{- range $key, $value := .Values.services.formioenterprise.deployment_nginx.config.secrets }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $value.name }}
                  key: {{ $value.key }}
            {{- end }}
            {{- range .Values.services.formioenterprise.deployment_nginx.config.env }}
            - name: {{ .name }}
              value: "{{ .value }}"
            {{- end }}
          resources:
            requests:
              memory: {{ .Values.services.formioenterprise.deployment_nginx.resources.requests.memory }}
              cpu: {{ .Values.services.formioenterprise.deployment_nginx.resources.requests.cpu }}
            limits:
              memory: {{ .Values.services.formioenterprise.deployment_nginx.resources.limits.memory }}
              cpu: {{ .Values.services.formioenterprise.deployment_nginx.resources.limits.cpu }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault

        - name: formio-enterprise
          image: "{{ .Values.services.formioenterprise.deployment.image.repository }}:{{ .Values.services.formioenterprise.deployment.image.tag }}"
          imagePullPolicy: {{ .Values.services.formioenterprise.deployment.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.services.formioenterprise.deployment.containerPort }}
              protocol: TCP
          volumeMounts:
            - name: tls-secret-volume
              mountPath: /etc/ssl/formio
              readOnly: true
            - name: redis-ca
              mountPath: /etc/ssl/redis
              readOnly: true
            - name: external-ca-bundle
              mountPath: /opt/ca/fed-ca.pem
              subPath: ca.pem
              readOnly: true
          env:
            {{- range .Values.services.formioenterprise.deployment.config.env }}
            - name: {{ .name }}
              value: "{{ .value }}"
            {{- end }}
            {{- range $key, $value := .Values.services.formioenterprise.deployment.config.secrets }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $value.name }}
                  key: {{ $value.key }}
            {{- end }}
          resources:
            limits:
              memory: {{ .Values.services.formioenterprise.deployment.resources.limits.memory }}
              cpu: {{ .Values.services.formioenterprise.deployment.resources.limits.cpu }}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 5000
            runAsGroup: 5000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: tls-secret-volume
          secret:
            secretName: cert-formio-enterprise
            items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
        - name: redis-ca
          secret:
            secretName:  cert-redis-4
            items:
              - key: tls.crt
                path: tls.crt
        - name: external-ca-bundle
          configMap:
            name: tls-external-ca-bundle
            items:
              - key: ca.pem
                path: ca.pem
{{ end }}
