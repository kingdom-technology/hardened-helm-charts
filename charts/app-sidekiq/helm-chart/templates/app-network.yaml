{{- if .Values.services.appsidekiq.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.services.appsidekiq.namespace }}
  labels:
    app: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/component: server
    rubix-app: {{ .Chart.Name }}
  {{- with .Values.services.appsidekiq.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.services.appsidekiq.service.type | default "ClusterIP" }}
  {{- if eq .Values.services.appsidekiq.service.type "LoadBalancer" }}
  loadBalancerClass: {{ .Values.services.appsidekiq.service.loadBalancerClass }}
  {{- end }}
  selector:
    app: {{ .Release.Name }}
  ports:
    - name: http-svc
      port: {{ .Values.services.appsidekiq.service.port }}
      targetPort: {{ .Values.services.appsidekiq.deployment_nginx.containerPort }}
      protocol: TCP
{{ end }}
